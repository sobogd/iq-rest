{
  "name": "@iq-rest/pricing",
  "version": "0.0.0",
  "private": true,
  "description": "Shared IQ Rest pricing — the à-la-carte price catalog + volume-discount + quote computation, single source of truth for the landing quiz, the dashboard constructor and Stripe ad-hoc checkout.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.0.5"
  },
  "files": [
    "dist"
  ]
}
