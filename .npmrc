# Flat node_modules so vite/next/nest/prisma resolve deps like they did under npm.
node-linker=hoisted
# Old per-app installs used --legacy-peer-deps; mirror that tolerance here.
strict-peer-dependencies=false
auto-install-peers=true
